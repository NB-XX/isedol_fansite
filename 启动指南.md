# 🚀 快速启动指南

## 第一次使用（必读）

### 1. 安装依赖

```bash
# 前端依赖（已完成 ✅）
cd web
npm install

# API 服务器依赖（已完成 ✅）
cd api-server
npm install
```

### 2. 启动服务

**重要：需要打开 3 个终端窗口**

#### 终端 1: 数据采集系统

```bash
# 在项目根目录
npm start
```

保持运行，不要关闭。

#### 终端 2: API 服务器

```bash
# 在项目根目录
cd api-server
npm start
```

保持运行，不要关闭。

#### 终端 3: 前端网站

```bash
# 在项目根目录
cd web
npm run dev
```

保持运行，不要关闭。

### 3. 访问网站

打开浏览器访问: **http://localhost:3000**

## 🎯 预期输出

### 终端 1 (数据采集)
```
[INFO] [CafeScraper] 已启用代理: http://127.0.0.1:7890
[INFO] [CafeScraper] 开始爬取文章
[INFO] [CafeScraper] 获取到 15 篇文章
[INFO] [StreamMonitor] 直播监控启动
```

### 终端 2 (API 服务器)
```
🚀 API 服务器运行在 http://localhost:8080
📁 数据目录: C:\...\data
📄 文章数据: C:\...\data\articles.json
📺 直播数据: C:\...\data\streams.json
```

### 终端 3 (前端网站)
```
VITE v5.0.11  ready in 500 ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

## ⚠️ 常见问题

### 问题 1: 'vite' 不是内部或外部命令

**原因**: 没有安装依赖

**解决**:
```bash
cd web
npm install
```

### 问题 2: ERR_CONNECTION_REFUSED

**原因**: 服务器没有启动

**解决**: 确保 3 个终端都在运行

### 问题 3: 端口被占用

**解决**:
```bash
# 查看占用端口的进程
netstat -ano | findstr :3000
netstat -ano | findstr :8080

# 结束进程
taskkill /PID <进程ID> /F
```

## 📝 启动顺序

1. ✅ 先启动数据采集系统（生成数据）
2. ✅ 再启动 API 服务器（读取数据）
3. ✅ 最后启动前端网站（显示数据）

## 🛑 停止服务

在每个终端按 `Ctrl + C` 停止服务。

## 🎉 成功标志

当你看到：
- ✅ 3 个终端都在运行
- ✅ 没有错误信息
- ✅ 浏览器可以打开 http://localhost:3000
- ✅ 页面显示主播头像和文章

恭喜！粉丝站已经成功运行！🎊
