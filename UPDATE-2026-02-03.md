# 更新日志 - 2026-02-03

## 新增功能

### 1. 作者统计优化 ✅

**问题**: 同一个人的不同账号（Naver Cafe 和 SOOP）被分开统计

**解决方案**:
- 添加了作者名称映射，自动合并同一个人的不同账号
- 在管理员控制台显示每个作者的 Naver 和 SOOP 文章数量

**映射规则**:
```javascript
{
  '아이네♪': '아이네',
  '고세구!': '고세구',
  '징버거!': '징버거',
  '징버거☆': '징버거',
  '주르르!': '주르르',
  '주르르_': '주르르',
  '릴파!': '릴파',
  '릴파♬': '릴파',
  '릴파 LILPA': '릴파',
  '비챤!': '비챤',
  '비챤_': '비챤'
}
```

**显示效果**:
```
고세구    | Naver:   3 | SOOP:  21 | 总计:  24
아이네    | Naver:   6 | SOOP:   8 | 总计:  14
릴파     | Naver:   6 | SOOP:  19 | 总计:  25
비챤     | Naver:   4 | SOOP:  17 | 总计:  21
징버거    | Naver:   4 | SOOP:   4 | 总计:   8
주르르    | Naver:   4 | SOOP:  13 | 总计:  17
```

### 2. 文章内容智能折叠功能 ✅

**问题**: 
- SOOP 公告栏和部分 Cafe 文章内容过长，影响浏览体验
- 之前所有文章都显示折叠按钮，包括短文章

**解决方案**:
- 添加了内容高度检测机制
- 只对实际高度超过 400px 的文章显示折叠功能
- 短文章不显示折叠按钮，保持完整显示

**技术实现**:
```javascript
// 检测内容高度
const checkContentHeight = (articleId) => {
  nextTick(() => {
    const contentEl = document.querySelector(`[data-article-id="${articleId}"] .article-content`)
    if (contentEl && contentEl.scrollHeight > 400) {
      collapsibleArticles.value.add(articleId)
    }
  })
}

// 判断是否需要折叠
const needsCollapse = (articleId) => {
  return collapsibleArticles.value.has(articleId)
}
```

**特性**:
- ✅ 自动检测内容实际高度
- ✅ 只对长文章显示折叠功能
- ✅ 短文章完整显示，无折叠按钮
- ✅ 平滑的展开/收起动画
- ✅ 渐变遮罩提示有更多内容
- ✅ 支持原文和翻译内容的折叠
- ✅ 每篇文章独立控制展开状态

**折叠逻辑**:
1. 文章加载后，检测每篇文章的 `scrollHeight`
2. 如果 `scrollHeight > 400px`，标记为可折叠
3. 可折叠的文章显示"展开全文"按钮
4. 短文章不显示任何折叠按钮

## 修改文件

### 后端
- `start-all.js`
  - 更新 `/api/admin/stats` 接口
  - 添加作者名称映射逻辑
  - 返回 Naver 和 SOOP 分别的统计数据

### 前端
- `web/src/views/Admin.vue`
  - 更新作者统计显示组件
  - 显示 Naver 和 SOOP 的文章数量标签
  
- `web/src/views/Home.vue`
  - 添加 `expandedArticles` 状态管理
  - 添加 `collapsibleArticles` 状态管理（新增）
  - 添加 `authorNameMap` 作者名称映射（新增）
  - 添加 `normalizeAuthorName` 方法（新增）
  - 添加 `checkContentHeight` 方法（新增）
  - 添加 `needsCollapse` 方法（新增）
  - 添加 `toggleExpand` 方法
  - 更新文章内容显示组件
  - 更新作者筛选逻辑，使用规范化名称
  - 更新标签显示，根据 source 显示不同文本
  - 添加展开/收起按钮和渐变遮罩
  - 只对需要折叠的文章显示按钮

## 视觉效果

### 作者统计卡片
```
┌─────────────────────────────────────┐
│ 👤 아이네                            │
│                                     │
│ [Naver: 6] [SOOP: 8]      [14 篇]  │
└─────────────────────────────────────┘
```

### 文章折叠效果

**长文章（高度 > 400px）**:
```
┌─────────────────────────────────────┐
│ 文章标题                             │
│                                     │
│ 文章内容...                          │
│ 文章内容...                          │
│ 文章内容...                          │
│ ▼▼▼ 渐变遮罩 ▼▼▼                    │
│     [展开全文 ▼]                     │
└─────────────────────────────────────┘
```

**短文章（高度 ≤ 400px）**:
```
┌─────────────────────────────────────┐
│ 文章标题                             │
│                                     │
│ 文章内容...                          │
│ 文章内容...                          │
│                                     │
│ （无折叠按钮）                       │
└─────────────────────────────────────┘
```

## 测试结果

### 作者统计
✅ 同一作者的不同账号已合并
✅ 正确显示 Naver 和 SOOP 的文章数量
✅ 总计数量正确

### 文章折叠
✅ 长文章自动折叠（高度 > 400px）
✅ 短文章完整显示，无折叠按钮
✅ 渐变遮罩显示正常
✅ 展开/收起功能正常
✅ 动画效果流畅
✅ 原文和翻译都支持折叠
✅ 高度检测准确

### 标签显示
✅ Naver 文章显示"Naver 咖啡厅"
✅ SOOP 文章显示"SOOP 公告栏"
✅ 标签颜色正确（Naver 绿色，SOOP 蓝色）

### 作者筛选
✅ 作者名称已规范化统一显示
✅ 点击作者头像筛选所有平台的文章
✅ 筛选标签显示规范化后的名称
✅ 测试：点击"아이네♪"筛选出 14 篇文章（Naver 6 + SOOP 8）
✅ 筛选后折叠功能正常工作
✅ 取消筛选后折叠功能正常工作
✅ 多次切换筛选折叠功能正常

## 使用说明

### 查看作者统计
1. 进入管理员控制台
2. 查看"作者统计"部分
3. 每个作者显示两个标签：
   - 绿色标签：Naver Cafe 文章数
   - 蓝色标签：SOOP 公告栏文章数
   - 右侧显示总计

### 使用文章折叠
1. 浏览首页文章列表
2. 长文章会自动折叠（高度超过 400px）
3. 短文章完整显示，无需折叠
4. 点击"展开全文"查看完整内容
5. 点击"收起"重新折叠
6. 翻译后的内容同样支持智能折叠

## 技术细节

### 高度检测机制
```javascript
// 在文章加载后检测高度
nextTick(() => {
  data.articles.forEach(article => {
    checkContentHeight(article.articleId)
  })
})

// 检测函数
const checkContentHeight = (articleId) => {
  nextTick(() => {
    const contentEl = document.querySelector(`[data-article-id="${articleId}"] .article-content`)
    if (contentEl && contentEl.scrollHeight > 400) {
      collapsibleArticles.value.add(articleId)
    }
  })
}
```

### 条件渲染
```vue
<!-- 只对需要折叠的文章显示按钮 -->
<div v-if="!isExpanded(article.articleId) && needsCollapse(article.articleId)">
  <button @click="toggleExpand(article.articleId)">展开全文</button>
</div>
```

## 下一步优化建议

1. ✅ 只对长文章显示折叠功能（已完成）
2. 可以考虑添加用户偏好设置，记住展开状态
3. 可以添加"全部展开"/"全部收起"的批量操作
4. 可以根据屏幕尺寸动态调整折叠阈值
5. 可以添加更多作者名称映射规则

## Bug 修复

### 修复 1：所有文章都显示折叠按钮
**问题**: 之前的实现对所有文章都显示折叠按钮，包括只有几句话的短文章

**原因**: 没有检测内容实际高度，直接对所有文章应用 `max-h-[400px]`

**解决**: 
- 添加了 `collapsibleArticles` Set 来跟踪需要折叠的文章
- 使用 `scrollHeight` 检测内容实际高度
- 只对高度超过 400px 的文章显示折叠功能

### 修复 2：标签显示问题
**问题**: Naver 文章显示"카페 스탭"而不是"Naver 咖啡厅"

**原因**: 使用了 `article.writer.memberLevelName` 字段，这是原始的韩文名称

**解决**: 
- 根据 `article.source` 字段显示标签
- `source === 'soop'` 显示"SOOP 公告栏"
- `source === 'naver'` 显示"Naver 咖啡厅"

### 修复 3：作者筛选问题
**问题**: 
- 同一个人在不同平台的名称不同（如 아이네♪ 和 아이네）
- 点击作者头像只会筛选一个平台的文章
- 作者名称显示不统一

**原因**: 
- 没有对作者名称进行规范化处理
- 筛选时使用原始名称进行精确匹配

**解决**: 
1. 添加作者名称映射表
2. 实现 `normalizeAuthorName` 函数统一名称
3. 筛选时使用规范化后的名称进行匹配
4. 显示时也使用规范化后的名称

**映射规则**:
```javascript
const authorNameMap = {
  '아이네♪': '아이네',
  '고세구!': '고세구',
  '징버거!': '징버거',
  '징버거☆': '징버거',
  '주르르!': '주르르',
  '주르르_': '주르르',
  '릴파!': '릴파',
  '릴파♬': '릴파',
  '릴파 LILPA': '릴파',
  '비챤!': '비챤',
  '비챤_': '비챤'
}
```

**效果**:
- 点击 "아이네♪" 或 "아이네" 都会筛选出该作者在所有平台的文章
- 所有文章显示统一的作者名称（如都显示"아이네"）
- 筛选标签也显示规范化后的名称

**测试结果**:
```
点击作者: 아이네♪
规范化为: 아이네
筛选结果: 14 篇文章
  Naver: 6 篇
  SOOP: 8 篇
```

### 修复 4：筛选后折叠功能失效
**问题**: 
- 点击作者筛选后，长文章不会自动折叠
- 取消筛选后，所有文章都不会自动折叠
- 只有刷新页面才能恢复正常

**原因**: 
- `filteredArticles` 改变时，没有重新检测文章高度
- `collapsibleArticles` 状态没有更新

**解决**: 
1. 添加 `watch(filteredArticles)` 监听筛选变化
2. 筛选改变时重新检测所有可见文章的高度
3. 使用 `setTimeout(100ms)` 确保 DOM 完全更新

**实现代码**:
```javascript
watch(filteredArticles, () => {
  // 当筛选改变时，重新检测所有可见文章的高度
  nextTick(() => {
    setTimeout(() => {
      filteredArticles.value.forEach(article => {
        checkContentHeight(article.articleId)
      })
    }, 100)
  })
}, { immediate: false })
```

**效果**:
- ✅ 筛选后长文章自动折叠
- ✅ 取消筛选后折叠功能正常
- ✅ 多次切换筛选都正常工作
- ✅ 无需刷新页面

---

**更新时间**: 2026-02-03
**版本**: v1.1.3
**状态**: 已完成并测试 ✅

## 更新历史

- v1.1.3 (2026-02-03): 修复筛选后折叠功能失效的问题
- v1.1.2 (2026-02-03): 修复标签显示和作者筛选问题
- v1.1.1 (2026-02-03): 修复智能折叠功能
- v1.1.0 (2026-02-03): 添加作者统计优化和文章折叠功能
